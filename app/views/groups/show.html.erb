<h1><%= @group.name %></h1>

<h2>Members</h2>
<% @group.users.each do |member| %>
  <p><%= link_to member.name, user_path(member) %></p>
  <%= render :partial => 'shared/instagram', :locals => { :user => member }%>
<% end %>

<% if @group.users.include?(current_user)%>
  <%= link_to 'edit', edit_group_path %>
<% end %>